import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const RequestAccountInfo = () => {
  return (
    <View>
      <Text>RequestAccountInfo</Text>
    </View>
  );
};

export default RequestAccountInfo;

const styles = StyleSheet.create({});
